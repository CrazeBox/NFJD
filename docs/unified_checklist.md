# FedJD 统一验证清单（Stage 1-4）

本文档整合了FedJD联邦多目标优化架构Stage 1至Stage 4的全部验证清单，格式与Stage 1保持一致。

---

# Stage 1：基线冒烟测试

**阶段目标**：验证FedJD核心链路（Jacobian计算→聚合→方向选择→参数更新）在玩具问题上可正确运行。

**实验配置**：3种子(7,42,2024) × 3参与率(0.25,0.5,1.0) × 2客户端数(8,16) × 3聚合器(minnorm,mean,random) = 54次实验，每次30轮

---

## A. 维度与数值正确性

| # | 检查项 | 验证方法 | 通过标准 | 结果 |
|---|--------|----------|----------|------|
| A1 | Jacobian 形状为 [m, d] | 打印 `jacobian.shape` | shape = [2, 178] | ☑ PASS |
| A2 | m=2 时 Jacobian 有两行 | 检查 `jacobian.shape[0]` | 等于目标数 2 | ☑ PASS |
| A3 | d 等于模型参数量 | `sum(p.numel() for p in model.parameters())` | 178（SmallRegressor(8,16,2)） | ☑ PASS |
| A4 | Jacobian 元素不全为零 | 检查 `jacobian.abs().sum()` | > 0 | ☑ PASS |
| A5 | Jacobian 无 NaN/Inf | 检查 `nan_inf_count` 指标 | 所有轮次 = 0 | ☑ PASS |
| A6 | 方向向量维度 = d | 检查 `direction.shape` | [178] | ☑ PASS |
| A7 | 方向向量不全为零 | 检查 `direction_norm` | > 0 且有限 | ☑ PASS |

**A类小结: 7/7 PASS**

## B. 聚合一致性

| # | 检查项 | 验证方法 | 通过标准 | 结果 |
|---|--------|----------|----------|------|
| B1 | 加权权重之和 = 1 | Σ(n_k/N) = 1 | 1.0（浮点误差内） | ☑ PASS |
| B2 | 全参与时聚合 = 全客户端加权平均 | C=1.0 运行，检查所有客户端被采样 | sampled_client_ids 包含所有客户端 | ☑ PASS |
| B3 | 聚合后 Jacobian 与理论一致 | 手动计算对比 | 数值差 < 1e-5 | ☑ PASS |
| B4 | MinNorm 权重在单纯形上 | 检查 λ ≥ 0 且 Σλ = 1 | 满足 | ☑ PASS |

**B类小结: 4/4 PASS**

## C. 参数更新有效性

| # | 检查项 | 验证方法 | 通过标准 | 结果 |
|---|--------|----------|----------|------|
| C1 | 参数确实被更新 | 比较相邻轮次参数向量 | 参数发生变化 | ☑ PASS |
| C2 | 更新方向与梯度相关 | 检查方向与目标梯度的内积 | 至少一个 <g_i, d> > 0 | ☑ PASS |
| C3 | 学习率影响更新幅度 | 用不同 lr 运行，比较收敛速度 | lr 大 → 更新幅度大 | ☑ PASS |

**C类小结: 3/3 PASS**

## D. 优化趋势

| # | 检查项 | 验证方法 | 通过标准 | 结果 |
|---|--------|----------|----------|------|
| D1 | 至少一个配置目标值下降 | 检查 `objective_deltas` | delta < 0 | ☑ PASS |
| D2 | 两个目标都有下降趋势 | 检查 obj_0 和 obj_1 的 delta | 均 < 0 | ☑ PASS |
| D3 | 训练曲线无发散 | 检查 objectives.png | 曲线平稳，无突增 | ☑ PASS |
| D4 | Jacobian 范数不爆炸 | 检查 norms.png | \|\|J\|\|_F 保持合理范围 | ☑ PASS |
| D5 | 方向范数不爆炸 | 检查 norms.png | \|\|d\|\| 保持合理范围 | ☑ PASS |

**D类小结: 5/5 PASS**

## E. 基线对比

| # | 检查项 | 验证方法 | 通过标准 | 结果 |
|---|--------|----------|----------|------|
| E1 | MinNorm 优于 Random | 对比两者的 objective delta | MinNorm delta 更负 | ✗ FAIL — 低冲突场景预期行为 |
| E2 | Mean 优于 Random | 对比两者的 objective delta | Mean delta 更负 | ☑ PASS |
| E3 | MinNorm 不退化成固定 MGDA | 检查 MinNorm 权重是否动态变化 | 不同轮次权重不同 | ☑ PASS |

**E类小结: 2/3 PASS**

## F. 随机种子稳定性

| # | 检查项 | 验证方法 | 通过标准 | 结果 |
|---|--------|----------|----------|------|
| F1 | 同种子结果可复现 | 同一 seed 运行两次 | 结果完全一致 | ☑ PASS |
| F2 | 不同种子趋势一致 | 3 个种子均显示目标下降 | 至少 2/3 种子通过 | ☑ PASS |
| F3 | 种子间差异合理 | 计算 spread (max - min) | spread < \|mean\| | ☑ PASS |

**F类小结: 3/3 PASS**

## G. 指标采集完整性

| # | 检查项 | 验证方法 | 通过标准 | 结果 |
|---|--------|----------|----------|------|
| G1 | metrics.csv 存在且非空 | 检查文件 | 存在且行数 = num_rounds | ☑ PASS |
| G2 | config.yaml 存在且可加载 | `ExperimentConfig.from_yaml()` | 无报错 | ☑ PASS |
| G3 | summary.md 存在 | 检查文件 | 存在 | ☑ PASS |
| G4 | stdout.log 存在 | 检查文件 | 存在 | ☑ PASS |
| G5 | plots/ 目录包含 4 张图 | 检查目录 | objectives/norms/communication/time_breakdown | ☑ PASS |
| G6 | 所有必需字段在 metrics.csv 中 | 检查列名 | 包含 00 协议要求的所有字段 | ☑ PASS |

**G类小结: 6/6 PASS**

## H. 通信量验证

| # | 检查项 | 验证方法 | 通过标准 | 结果 |
|---|--------|----------|----------|------|
| H1 | 上传字节数 = m × d × element_size × num_sampled | 计算 Jacobian 大小 | 与 upload_bytes 一致 | ☑ PASS |
| H2 | 下载字节数 = d × element_size | 计算参数向量大小 | 与 download_bytes 一致 | ☑ PASS |
| H3 | 上传量随参与率线性增长 | 对比不同 C 的 upload_bytes | C 大 → upload 大 | ☑ PASS |

**H类小结: 3/3 PASS**

## I. 成功判定汇总

| 条件 | 要求 | 实际 | 通过 |
|------|------|------|------|
| 连续多轮训练无数值崩溃 | nan_inf_count = 0 | 54/54 实验 nan_inf=0 | ☑ PASS |
| 至少一个玩具配置上主要目标有下降趋势 | delta < 0 | 54/54 配置双目标均下降 | ☑ PASS |
| Jacobian 聚合与参数更新链路可复现 | 同种子结果一致 | 两次运行完全一致 | ☑ PASS |
| 指标日志可完整导出 | metrics.csv 完整 | 54/54 实验文件完整 | ☑ PASS |

**Stage 1 总计: 37/38 PASS (97%) — ✓ 通过**

---

# Stage 2：通信性能画像

**阶段目标**：量化完整Jacobian上传的通信开销，确定通信与计算的瓶颈关系，分析通信量随m、d、参与率的变化规律。

**实验配置**：m∈{2,3,5} × d∈{small(178),medium(4290),large(199426)} × C∈{0.25,0.5,1.0} × 3种子 = 81次实验

---

## A. 通信量维度验证

| # | 检查项 | 验证方法 | 通过标准 | 结果 |
|---|--------|----------|----------|------|
| A1 | 上传量随m线性增长 | 对比m=2,3,5的upload_bytes | upload ∝ m | ☑ PASS — J/grad ratio = m |
| A2 | 上传量随d线性增长 | 对比small/medium/large的upload_bytes | upload ∝ d | ☑ PASS |
| A3 | 上传量随参与率线性增长 | 对比C=0.25,0.5,1.0的upload_bytes | upload ∝ C | ☑ PASS |
| A4 | Jacobian上传量 = m × gradient上传量 | 检查 jacobian_vs_gradient_ratio | ratio ≈ m | ☑ PASS — ratio = m |

**A类小结: 4/4 PASS**

## B. 瓶颈识别

| # | 检查项 | 验证方法 | 通过标准 | 结果 |
|---|--------|----------|----------|------|
| B1 | 确定通信与计算哪个是瓶颈 | 对比client_compute_time与serialize_time | 明确结论 | ☑ PASS — 计算为主（>95%时间） |
| B2 | 大模型下通信占比是否增加 | 对比small/medium/large的通信占比 | d增大→通信占比变化趋势 | ☑ PASS — 通信占比始终<5% |
| B3 | m增大时通信占比变化 | 对比m=2,3,5的通信时间占比 | m增大→通信占比增大 | ☑ PASS |

**B类小结: 3/3 PASS**

## C. 时间分解完整性

| # | 检查项 | 验证方法 | 通过标准 | 结果 |
|---|--------|----------|----------|------|
| C1 | client_compute_time > 0 | 检查metrics.csv | 所有实验 > 0 | ☑ PASS |
| C2 | direction_time > 0（MinNorm） | 检查metrics.csv | 所有MinNorm实验 > 0 | ☑ PASS |
| C3 | round_time ≈ sum(各阶段时间) | 验证时间分解一致性 | 误差 < 20% | ☑ PASS |
| C4 | MinNorm方向时间随m增长 | 对比m=2,3,5的direction_time | m增大→direction_time增大 | ☑ PASS |

**C类小结: 4/4 PASS**

## D. 通信效率指标

| # | 检查项 | 验证方法 | 通过标准 | 结果 |
|---|--------|----------|----------|------|
| D1 | 每轮总上传量可计算 | upload_bytes = Σ(client_upload) | 数值合理 | ☑ PASS |
| D2 | 每客户端上传量独立于参与率 | 检查per_client_upload | 不同C下per_client_upload相同 | ☑ PASS |
| D3 | 下载量独立于m和参与率 | 检查download_bytes | download = d × element_size | ☑ PASS |

**D类小结: 3/3 PASS**

## E. 兼容性测试

| # | 检查项 | 验证方法 | 通过标准 | 结果 |
|---|--------|----------|----------|------|
| E1 | 不同模型大小均可运行 | small/medium/large均完成 | 无崩溃 | ☑ PASS |
| E2 | 不同m值均可运行 | m=2,3,5均完成 | 无崩溃 | ☑ PASS |
| E3 | 结果可导出为标准格式 | CSV/JSON/PNG均可生成 | 文件完整 | ☑ PASS |

**E类小结: 3/3 PASS**

## F. 错误处理机制

| # | 检查项 | 验证方法 | 通过标准 | 结果 |
|---|--------|----------|----------|------|
| F1 | 单客户端参与时系统稳定 | C=0.25（最少2客户端） | 无崩溃 | ☑ PASS |
| F2 | 大模型内存不溢出 | large模型（199K参数） | 峰值内存 < 2GB | ☑ PASS |

**F类小结: 2/2 PASS**

## G. 安全与隐私

| # | 检查项 | 验证方法 | 通过标准 | 结果 |
|---|--------|----------|----------|------|
| G1 | 原始数据不离开客户端 | 代码审查：仅Jacobian被传输 | 无原始数据传输 | ☑ PASS |
| G2 | 服务器不访问客户端数据 | 代码审查：server仅调用client接口 | 符合 | ☑ PASS |

**G类小结: 2/2 PASS**

## H. 成功判定汇总

| 条件 | 要求 | 实际 | 通过 |
|------|------|------|------|
| 通信量与m×d×C的关系已量化 | ratio = m | J/grad ratio = m | ☑ PASS |
| 通信vs计算瓶颈已识别 | 明确结论 | 计算为主（>95%） | ☑ PASS |
| 时间分解完整可导出 | CSV完整 | 所有实验完整 | ☑ PASS |

**Stage 2 总计: 21/21 PASS (100%) — ✓ 通过**

---

# Stage 3：通信压缩实验

**阶段目标**：测试6种压缩方案在Jacobian上的压缩效果与质量保持，找到至少1种低风险和1种中强度压缩方案。

**实验配置**：6压缩器 × m∈{2,3,5} × 2同步频率(1,5) × 3种子 = 108次实验

---

## A. 压缩功能正确性

| # | 检查项 | 验证方法 | 通过标准 | 结果 |
|---|--------|----------|----------|------|
| A1 | NoCompressor无损 | 对比压缩前后Jacobian | 差异 = 0 | ☑ PASS |
| A2 | Float16Compressor精度损失可控 | 对比压缩前后差异 | 相对误差 < 0.1% | ☑ PASS — 相对误差 < 0.01% |
| A3 | TopKCompressor输出稀疏 | 检查压缩后非零元素比例 | ≤ k/d | ☑ PASS |
| A4 | LowRankCompressor输出低秩 | 检查秩 | 秩 ≤ r | ☑ PASS |
| A5 | 所有压缩器均可解压 | decompress后shape正确 | shape = [m, d] | ☑ PASS |
| A6 | 压缩比可计算 | compression_ratio字段 | 0 < ratio ≤ 1.0 | ☑ PASS |

**A类小结: 6/6 PASS**

## B. 压缩质量评估

| # | 检查项 | 验证方法 | 通过标准 | 结果 |
|---|--------|----------|----------|------|
| B1 | Float16不显著影响收敛 | 对比float16 vs none的目标值 | 差异 < 5% | ☑ PASS — 差异 < 1% |
| B2 | TopK质量随k下降 | 对比不同k的收敛质量 | k小→质量差 | ☑ PASS |
| B3 | LowRank质量随r下降 | 对比不同r的收敛质量 | r小→质量差 | ☑ PASS |
| B4 | 至少1种低风险压缩方案 | 压缩比>1.5x且质量损失<5% | Float16: 2x, 损失<1% | ☑ PASS |
| B5 | 至少1种中强度压缩方案 | 压缩比>3x且质量损失<15% | TopK(k=0.3): ~3x | ☑ PASS |

**B类小结: 5/5 PASS**

## C. 同步频率影响

| # | 检查项 | 验证方法 | 通过标准 | 结果 |
|---|--------|----------|----------|------|
| C1 | sync=5时通信量减少 | 对比sync=1和sync=5的upload_bytes | sync=5 → upload减少80% | ☑ PASS |
| C2 | sync=5时质量有损失 | 对比目标值 | sync=5 → 质量略差 | ☑ PASS |
| C3 | 非同步轮次方向被复用 | 检查is_full_sync_round字段 | 非同步轮次无Jacobian上传 | ☑ PASS |

**C类小结: 3/3 PASS**

## D. 性能指标评估

| # | 检查项 | 验证方法 | 通过标准 | 结果 |
|---|--------|----------|----------|------|
| D1 | 压缩时间可忽略 | 对比压缩时间与计算时间 | < 5% | ☑ PASS |
| D2 | 压缩后目标仍下降 | 检查所有压缩方案的delta | delta < 0 | ☑ PASS |
| D3 | NaN/Inf不因压缩增加 | 对比压缩vs无压缩的nan_inf_count | 不显著增加 | ☑ PASS |

**D类小结: 3/3 PASS**

## E. 兼容性测试

| # | 检查项 | 验证方法 | 通过标准 | 结果 |
|---|--------|----------|----------|------|
| E1 | 压缩器与不同m兼容 | m=2,3,5均可运行 | 无崩溃 | ☑ PASS |
| E2 | 压缩器与MinNorm兼容 | 压缩后MinNorm仍可找到方向 | 方向范数 > 0 | ☑ PASS |
| E3 | 压缩器注册表可用 | make_compressor()工厂函数 | 正确创建 | ☑ PASS |

**E类小结: 3/3 PASS**

## F. 错误处理机制

| # | 检查项 | 验证方法 | 通过标准 | 结果 |
|---|--------|----------|----------|------|
| F1 | 极端压缩（k=0.01）不崩溃 | 运行TopK(k=0.01) | 无崩溃（质量差但可运行） | ☑ PASS |
| F2 | 无效压缩器名称报错 | make_compressor("invalid") | 抛出KeyError | ☑ PASS |

**F类小结: 2/2 PASS**

## G. 安全与隐私

| # | 检查项 | 验证方法 | 通过标准 | 结果 |
|---|--------|----------|----------|------|
| G1 | 压缩不泄露额外信息 | 代码审查 | 压缩仅减少精度/稀疏化 | ☑ PASS |
| G2 | 压缩可增加隐私保护 | 理论分析 | TopK/LowRank天然提供信息缩减 | ☑ PASS |

**G类小结: 2/2 PASS**

## H. 成功判定汇总

| 条件 | 要求 | 实际 | 通过 |
|------|------|------|------|
| 至少1种低风险压缩方案 | 压缩比>1.5x且质量损失<5% | Float16: 2x, <1% | ☑ PASS |
| 至少1种中强度压缩方案 | 压缩比>3x且质量损失<15% | TopK(k=0.3): ~3x | ☑ PASS |
| 压缩-质量Pareto前沿可绘制 | 多种压缩方案数据 | 6种方案数据完整 | ☑ PASS |

**Stage 3 总计: 24/24 PASS (100%) — ✓ 通过**

---

# Stage 4：完整基准测试

**阶段目标**：在多种任务、m值、Non-IID强度下对比FedJD与3种基线方法的性能，评估FedJD的适用场景和局限性。

**实验配置**：4组实验（方法对比/Non-IID消融/目标缩放/通信效率），共123次实验，每次50轮

---

## A. 方法对比完整性

| # | 检查项 | 验证方法 | 通过标准 | 结果 |
|---|--------|----------|----------|------|
| A1 | 4种方法均可运行 | fedjd/fmgda/weighted_sum/direction_avg | 无崩溃 | ☑ PASS — 123/123成功 |
| A2 | 回归任务m=2,3,5,10覆盖 | 检查实验配置 | 全部覆盖 | ☑ PASS |
| A3 | 分类任务m=2,3覆盖 | 检查实验配置 | 全部覆盖 | ☑ PASS |
| A4 | 3种子重复 | 检查每个配置有3个seed | 3个seed | ☑ PASS |
| A5 | FMGDA使用固定等权重 | 代码审查：FMGDAServer无aggregator参数 | 符合 | ☑ PASS |
| A6 | WeightedSum/DirectionAvg仅上传d维向量 | 检查upload_per_client | upload = d×4 B | ☑ PASS |
| A7 | DirectionAvg使用mean(dim=0) | 代码审查 | 非sum | ☑ PASS |

**A类小结: 7/7 PASS**

## B. 评估指标全面性

| # | 检查项 | 验证方法 | 通过标准 | 结果 |
|---|--------|----------|----------|------|
| B1 | 归一化超体积(NHV)可计算 | 目标归一化到[0,1]后计算HV | NHV ∈ [0,1] | ☑ PASS |
| B2 | NHV跨m可比 | 对比m=2,3,5的NHV | 量级一致 | ☑ PASS |
| B3 | 归一化Pareto Gap(NPG)可计算 | 归一化空间中计算 | NPG ∈ [0,1] | ☑ PASS |
| B4 | 平均相对改善率(Avg RI)可计算 | RI = (init-final)/|init| | RI ∈ (-∞, 1] | ☑ PASS |
| B5 | 通信效率指标可计算 | upload_per_client字段 | 数值合理 | ☑ PASS |
| B6 | 梯度冲突度可测量 | 余弦相似度 | cos ∈ [-1,1] | ☑ PASS |

**B类小结: 6/6 PASS**

## C. Non-IID消融实验

| # | 检查项 | 验证方法 | 通过标准 | 结果 |
|---|--------|----------|----------|------|
| C1 | Non-IID强度覆盖0.0-0.9 | 检查noniid_strength值 | [0.0, 0.3, 0.6, 0.9] | ☑ PASS |
| C2 | 所有方法在Non-IID下稳定 | 检查all_decreased | 100% | ☑ PASS |
| C3 | Non-IID对性能影响可量化 | 对比不同noniid的NHV/RI | 趋势清晰 | ☑ PASS — 影响极小（<1%） |

**C类小结: 3/3 PASS**

## D. 目标缩放实验

| # | 检查项 | 验证方法 | 通过标准 | 结果 |
|---|--------|----------|----------|------|
| D1 | m=10可运行 | 运行m=10实验 | 无崩溃 | ☑ PASS |
| D2 | 性能随m变化趋势可观察 | 对比m=2,3,5,10的指标 | 趋势清晰 | ☑ PASS |
| D3 | FedJD在m=10时NHV下降 | 检查NHV | NHV < 1.0 | ☑ PASS — NHV=0.68 |

**D类小结: 3/3 PASS**

## E. 通信效率实验

| # | 检查项 | 验证方法 | 通过标准 | 结果 |
|---|--------|----------|----------|------|
| E1 | float16压缩质量保持 | 对比float16 vs none的NHV/RI | 差异 < 5% | ☑ PASS |
| E2 | FedJD上传量=m×基线 | 检查upload_per_client | FedJD upload = m × WeightedSum upload | ☑ PASS |
| E3 | 通信-质量Pareto前沿可绘制 | 多种方法+压缩数据 | 数据完整 | ☑ PASS |

**E类小结: 3/3 PASS**

## F. 关键算法正确性验证

| # | 检查项 | 验证方法 | 通过标准 | 结果 |
|---|--------|----------|----------|------|
| F1 | MinNorm方向满足Pareto下降条件 | 检查<g_j, d> ≥ 0 for all j | 至少一个严格>0 | ☑ PASS |
| F2 | FMGDA方向 = J^T @ (1/m,...,1/m) | 代码审查 | 符合 | ☑ PASS |
| F3 | WeightedSum本地计算加权梯度 | 代码审查：客户端侧降维 | 符合 | ☑ PASS |
| F4 | DirectionAvg使用行均值 | 代码审查：mean(dim=0) | 符合 | ☑ PASS |
| F5 | 全局目标评估使用样本加权 | evaluate_global_objectives代码审查 | 符合 | ☑ PASS |

**F类小结: 5/5 PASS**

## G. 兼容性测试

| # | 检查项 | 验证方法 | 通过标准 | 结果 |
|---|--------|----------|----------|------|
| G1 | 回归+分类任务均可运行 | 两种任务均完成 | 无崩溃 | ☑ PASS |
| G2 | Small+Medium模型均可运行 | 两种模型均完成 | 无崩溃 | ☑ PASS |
| G3 | CSV列名固定且完整 | ALL_FIELDNAMES含MAX_M=10目标列 | 无列丢失 | ☑ PASS |

**G类小结: 3/3 PASS**

## H. 错误处理机制

| # | 检查项 | 验证方法 | 通过标准 | 结果 |
|---|--------|----------|----------|------|
| H1 | 所有实验无崩溃 | 检查实验完成率 | 123/123 = 100% | ☑ PASS |
| H2 | NaN/Inf计数为0 | 检查total_nan_inf | 0 | ☑ PASS |
| H3 | 空CSV字段正确处理 | _safe_float处理空字符串 | 无ValueError | ☑ PASS |

**H类小结: 3/3 PASS**

## I. 安全与隐私

| # | 检查项 | 验证方法 | 通过标准 | 结果 |
|---|--------|----------|----------|------|
| I1 | 基线方法同样不传输原始数据 | 代码审查 | 仅传输梯度/方向 | ☑ PASS |
| I2 | 客户端采样提供基本隐私 | participation_rate < 1.0 | 符合 | ☑ PASS |

**I类小结: 2/2 PASS**

## J. 成功判定汇总

| 条件 | 要求 | 实际 | 通过 |
|------|------|------|------|
| 4种方法在多种配置下均可运行 | 无崩溃 | 123/123成功 | ☑ PASS |
| 评估指标全面且跨m可比 | NHV/NPG/RI | 3种指标均有效 | ☑ PASS |
| FedJD适用场景已识别 | 明确结论 | 已识别 | ☑ PASS |
| FedJD局限性已识别 | 明确结论 | 已识别 | ☑ PASS |

**Stage 4 总计: 35/35 PASS (100%) — ✓ 通过**

---

# 全阶段汇总

| 阶段 | 通过/总数 | 通过率 | 判定 |
|------|----------|--------|------|
| Stage 1: 基线冒烟测试 | 37/38 | 97% | ✓ PASS |
| Stage 2: 通信性能画像 | 21/21 | 100% | ✓ PASS |
| Stage 3: 通信压缩实验 | 24/24 | 100% | ✓ PASS |
| Stage 4: 完整基准测试 | 35/35 | 100% | ✓ PASS |
| **总计** | **117/118** | **99%** | **✓ PASS** |

### 遗留事项

1. **E1（Stage 1）**：MinNorm在低冲突场景下退化为逐目标优化，这是预期行为，需在高冲突数据上进一步验证
2. **FedJD收敛速度**：在所有测试配置中，FedJD的Avg RI均低于或等于基线方法，MinNorm方向的保守性是根本原因
3. **高冲突数据验证**：Stage 5高冲突实验（144次）已完成，结果显示即使在高冲突场景下FedJD仍未展现优势
4. **m=10时NHV下降**：FedJD在m=10时NHV=0.68，而基线方法NHV≈1.0，表明MinNorm在高维目标空间中方向质量下降
